syntax = "proto3";

package horus_bridge;

enum MessageType {
  INVALID = 0; // It's common to have an INVALID or UNKNOWN option
  STRING = 1;  // Notice we assign a value
  // ... Other message types (can be added later)
}

message Message {
  MessageType type = 1;
  string data = 2; 
}
